#!/usr/bin/env bash
set -euo pipefail

echo "==> Installing tools via mise..."
mise install

echo "==> Initializing theme submodule..."
git submodule update --init --recursive

echo "==> Installing Python dependencies..."
uv sync

echo "==> Setup complete! Run 'hugo server -D' to start the dev server."
